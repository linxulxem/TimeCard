<Window x:Class="NfcTimeCard.Views.CameraCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="カメラ撮影" Height="650" Width="500" 
        WindowStartupLocation="CenterOwner" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent"
        Closed="Window_Closed">

    <Border CornerRadius="25" Background="White" BorderBrush="#e2e8f0" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="顔写真の撮影" FontSize="20" FontWeight="Bold" Foreground="#1e293b" HorizontalAlignment="Center" Margin="0,10,0,20"/>

            <Border Grid.Row="1" Width="360" Height="480" Background="#0f172a" CornerRadius="15" ClipToBounds="True" BorderBrush="#4f46e5" BorderThickness="2">
                <Grid>
                    <Image x:Name="ImgPreview" Stretch="UniformToFill"/>
                    <Ellipse Width="250" Height="320" Stroke="White" StrokeThickness="2" StrokeDashArray="4 2" Opacity="0.4" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,25,0,10">
                <Button Content="キャンセル" Click="Cancel_Click" Width="100" Height="50" Background="Transparent" Foreground="#94a3b8" BorderThickness="0" Cursor="Hand" Margin="0,0,20,0"/>
                <Button x:Name="BtnCapture" Content="📷 撮影" Click="Capture_Click" Width="180" Height="50" Style="{StaticResource ActionButtonStyle}" FontSize="18"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>