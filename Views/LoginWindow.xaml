<Window x:Class="NfcTimeCard.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="520" Width="400"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True" WindowStyle="None" Background="Transparent">

    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#6366f1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,15"/> 
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4f46e5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernInputStyle" TargetType="Control">
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,8,0,24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border x:Name="border" BorderBrush="#d1d5db" BorderThickness="1.5" CornerRadius="10" Background="White">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="10,0" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6366f1"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="25" MouseLeftButtonDown="Window_MouseLeftButtonDown">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#4f46e5" Offset="0"/>
                <GradientStop Color="#7c3aed" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        
        <Grid Margin="30">
            <Border Background="White" CornerRadius="20" Padding="35">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.1"/>
                </Border.Effect>
                
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Administrator" FontSize="26" FontWeight="Bold" Foreground="#1f2937" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock Text="管理者IDとパスワードを入力してください" FontSize="11" Foreground="#6b7280" HorizontalAlignment="Center" Margin="0,0,0,35"/>

                    <TextBlock Text="管理者ID" FontSize="12" FontWeight="SemiBold" Foreground="#4b5563" Margin="4,0"/>
                    <TextBox x:Name="TxtID" Style="{StaticResource ModernInputStyle}"/>

                    <TextBlock Text="パスワード" FontSize="12" FontWeight="SemiBold" Foreground="#4b5563" Margin="4,0"/>
                    <PasswordBox x:Name="TxtPassword" Style="{StaticResource ModernInputStyle}"/>

                    <Button Content="ログイン" Style="{StaticResource ModernButtonStyle}" Click="LoginButton_Click" IsDefault="True"/> 

                    <Button Content="キャンセル" 
                            Background="Transparent" 
                            BorderThickness="0" 
                            Foreground="#94a3b8" 
                            FontSize="12" 
                            Margin="0,20,0,0" 
                            Click="CancelButton_Click" 
                            Cursor="Hand"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>